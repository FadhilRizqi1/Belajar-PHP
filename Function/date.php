<?php
    //Menampilkan data dengan format tertentu
    // echo date("l, d-M-Y");

    //Menemukan hari setelah waktu tertentu
    // echo date("l", time()+2*24*60*60*24);

    //Menemukan detik menggunakan mktime
    //mktime(0,0,0,0,0,0);
    //detik, menit, jam, tanggal, bulan, tahun

    // echo mktime(0,0,0,1,5,2006);

    // Menemukan hari menggunakan date
    // echo date("l", time()+1136415600);
    // echo date("l", mktime(0,0,0,1,5,2006));
?>